[build]
  publish = "public"
  functions = "netlify/functions"

# CSP frame-ancestors temporarily removed to allow Wix app web view.
# Will restore with correct origin once we identify it from logs.
[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
